<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/spotify_white"/> <!-- Background color of EditText -->
    <corners android:radius="10dp"/> <!-- Radius for rounded corners -->
    <!-- Optional: Define stroke -->
    <stroke
        android:width="0dp"
        android:color="#000000"/> <!-- Border color -->
    <!-- Optional: Add padding if needed -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>
